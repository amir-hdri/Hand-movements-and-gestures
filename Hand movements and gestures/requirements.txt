# NOTE: This project currently relies on the TensorFlow/Keras 2.x ecosystem.
# Newer environments often pull NumPy 2.x and Keras 3.x, which break the shipped
# `.h5` model and/or compiled wheels (MediaPipe / TensorFlow).

# ---- Core runtime (known-good) ----
numpy>=1.26.0,<2.0.0
opencv-contrib-python==4.8.1.78
mediapipe==0.10.14
attrs==25.4.0

# Matplotlib is a transitive dependency of mediapipe; pin to a NumPy 1.x-compatible version.
matplotlib==3.7.5
contourpy==1.1.1

# ---- TensorFlow / Keras (model inference) ----
# TensorFlow 2.13 is compatible with the pretrained model shipped in ./models.
tensorflow>=2.16.1
tf_keras>=2.16.0

# ---- Training (optional; used in train.ipynb) ----
scipy>=1.11.0
scikit-learn>=1.3.2

# ---- PingPong robot integration (optional) ----
pyserial==3.5

# ---- GUI / Dashboard ----
fastapi==0.103.2
uvicorn==0.23.2
python-multipart==0.0.6
websockets==11.0.3
